<%= form_with model: @review,url: reviews_show_path, local: true do |f| %>

    <%= f.hidden_field :user_id, @user.id value: @user.id %>
    <%= f.text_field :body, class: "text-field" %>
    <%= f.submit '投稿', class: "submit-btn" %>
<% end %>