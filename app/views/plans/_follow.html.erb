<%= form_for(current_user, url: user_relationships_path(@user), method: :post, remote: true) do |f| %>
    <%= hidden_field_tag :user_id, @user.id %>
    <%= f.submit "フォローする", class: "waves-light btn follow_suru" %>
<% end %>