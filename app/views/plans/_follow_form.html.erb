<% if user_signed_in? && @user != current_user %>
    <div id="follow_form">
        <% if current_user.followed_by?(@user) %>
        <%= render "plans/unfollow" %>
        <% else %>
        <%= render "plans/follow" %>
        <% end %>
    </div>
<% end %>